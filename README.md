# LibreEcho Web Interface Prototype

Open `index.html` in a browser. The prototype is entirely static and uses no external dependencies.

Implemented views include Overview, Device, Audio, Wake Word, LED & Buttons, Network, Privacy, Integrations, System, Logs and About. Navigation, switches, colour selectors, sliders and save feedback are interactive.
