# Copy to .env (gitignored) or export these before running.
# Device under test:
BASE_URL=http://localhost:8080
# Login credentials (device local user):
LE_USER=lpick
LE_PASS=
# Opt into hardware-actuating self-tests (lights the LED ring). Default off.
# RUN_HW_TESTS=1
