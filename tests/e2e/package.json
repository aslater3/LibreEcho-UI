{
  "name": "libreecho-ui-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests for the LibreEcho Control Centre UI, run against a live device by IP.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
