'use strict';
const ref=document.querySelector('#api-reference'),filter=document.querySelector('#endpoint-filter');
let operations=[];
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){const query=filter.value.trim().toLowerCase();const shown=operations.filter(x=>`${x.method} ${x.path} ${x.summary} ${x.tag}`.toLowerCase().includes(query));const groups=new Map();shown.forEach(x=>{if(!groups.has(x.tag))groups.set(x.tag,[]);groups.get(x.tag).push(x)});ref.innerHTML=[...groups].map(([tag,items])=>`<section class="tag"><h2>${esc(tag)}</h2>${items.map(x=>`<details class="operation ${x.method.toLowerCase()}"><summary><span class="method">${x.method}</span><code>${esc(x.path)}</code><span>${esc(x.summary)}</span><b>⌄</b></summary><div class="operation-body"><p>${esc(x.description||x.summary)}</p>${x.headers.length?`<h3>Required headers</h3><ul>${x.headers.map(h=>`<li><code>${esc(h)}</code></li>`).join('')}</ul>`:''}${x.requestBody?'<h3>Request body</h3><code>Content-Type: application/json</code>':''}<h3>Responses</h3><div class="responses">${x.responses.map(r=>`<span><b>${r.code}</b> ${esc(r.description)}</span>`).join('')}</div></div></details>`).join('')}</section>`).join('')||'<div class="empty">No matching endpoints.</div>';document.querySelector('#endpoint-count').textContent=`${shown.length} endpoint${shown.length===1?'':'s'}`}
fetch('/openapi.json').then(r=>{if(!r.ok)throw new Error('Specification unavailable');return r.json()}).then(spec=>{document.querySelector('#api-title').textContent=spec.info.title;document.querySelector('#api-version').textContent=spec.info.version;document.querySelector('#api-description').textContent=spec.info.description;document.querySelector('#base-url').textContent=spec.servers[0].url;Object.entries(spec.paths).forEach(([path,item])=>Object.entries(item).forEach(([method,op])=>{if(!['get','put','post','delete','patch'].includes(method))return;operations.push({method:method.toUpperCase(),path:spec.servers[0].url+(path==='/'?'':path),summary:op.summary||'',description:op.description||'',tag:(op.tags||['Other'])[0],headers:(op.parameters||[]).filter(p=>p.in==='header').map(p=>p.name),requestBody:!!op.requestBody,responses:Object.entries(op.responses||{}).map(([code,r])=>({code,description:r.description||'Response'}))})}));render()}).catch(e=>{ref.innerHTML=`<div class="empty error">${esc(e.message)}</div>`});
filter.addEventListener('input',render);document.querySelector('#copy-base').onclick=()=>navigator.clipboard.writeText(location.origin+document.querySelector('#base-url').textContent);
