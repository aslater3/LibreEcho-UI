# LibreEcho public UTC sources.
# Keep no more than four peers. A validated persistent replacement may be
# stored at /data/libreecho/config/ntp.conf.
server time.cloudflare.com
server time.nist.gov
server ntp1.npl.co.uk
server ntp2.npl.co.uk
